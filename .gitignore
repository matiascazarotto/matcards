# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*.swo

# Node (caso adicione build futuramente)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Firebase
.firebase/
firebase-debug.log
firebase-debug.*.log

# Local build artifacts
dist/
build/

# Backups locais do user (não devem ir pro repo)
*-backup-*.json

# .apkg sources — convertidos via tools/apkg-to-matcards.js pra data/deck-*.json
# Binários grandes (centenas de MB com áudio/imagem) não vão pro repo
data/apkg/
